spring:
  application:
    name: Net Service
  profiles:
    active: dev
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:

  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: url
    username: username
    password: password
    spring:
      jackson:
        serialization:
          WRITE_DATES_AS_TIMESTAMPS: false
